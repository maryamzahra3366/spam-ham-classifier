# spam-ham-classifier
A simple machine learning project to classify text messages as spam or ham using Python and scikit-learn.
Dataset

CSV file with two columns: label (spam/ham) and text (message content).

## Usage

1. Open the Jupyter Notebook:

```bash
jupyter notebook SpamClassifier.ipynb
Follow the cells in order:

Load dataset

Split data

Vectorize text

Train models and tune hyperparameters

Evaluate models

Find the best model

The notebook prints the best model at the end:

Best model: RandomForestClassifier


4. **Push to GitHub**  
   - Create a new repository.  
   - Copy your folder (`spam-classifier`) into it.  
   - Commit and push:

```bash
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/maryamzahra3366/spam-classifier.git
git push -u origin main
